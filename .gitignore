__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
venv/
.venv/
# conda env when FLOWCEPT_ENV_PREFIX is unset and it falls back into the repo
envs/

# Captured provenance artifacts (land in each exercise dir when it is run)
flowcept_buffer*.jsonl
*_card.md
provenance_perf_*.csv
langgraph_provenance_perf_*.csv
runs/
vllm_server.log
flowcept_lmdb/
runinfo/
.ipynb_checkpoints/

# Cached local LLM weights (Aurora: pre-cached on a login node)
hf_cache/

# LaTeX build artifacts
slides/*.aux
slides/*.log
slides/*.nav
slides/*.out
slides/*.snm
slides/*.toc
slides/*.vrb

# Keep the shipped provenance sample
!provenance/sample/
!provenance/sample/*
.DS_Store
