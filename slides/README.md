# Slides (LaTeX Beamer)

```bash
cd slides && make      # -> flowcept_academy.pdf  (needs pdflatex + beamer + metropolis)
```

~15 frames, provenance-focused. Fully self-contained (no image includes) — the
"dashboard" is shown as the tutorial's actual terminal ASCII output.
